# Overview

This is a pnpm workspace monorepo using TypeScript, designed to build a robust and scalable application. The project aims to provide a reliable backend API with a focus on multi-tenancy, enterprise standards, and a deterministic agent-based system. Key capabilities include a SQLite-backed API server, a multi-stage agent loop, a tool registry, an approval system, comprehensive privacy logging, and local authentication.

The core vision is to deliver a high-performance, secure, and maintainable platform.

# User Preferences

I expect all task agents to strictly adhere to the planning documents located at `.local/tasks/OMNINITY_PROJECT_CONTEXT.md` and `.local/tasks/OMNINITY_BUG_PREVENTION_STANDARDS.md`. These documents are the single source of truth, and no deviations are permitted without explicit architect review.

# System Architecture

The system is built on a pnpm workspace monorepo, utilizing Node.js 24 and TypeScript 5.9. The API is powered by Express 5, with data managed by SQLite and Drizzle ORM. Zod is used for validation, and Orval handles API codegen from an OpenAPI specification. The build process uses esbuild.

## Core Architectural Patterns

- **Monorepo Structure**: Organized into shared packages with a defined build order: `@workspace/types` → `@workspace/db` → `@workspace/api-zod` → `@workspace/api-client-react` → consumers.
- **Multi-Tenancy**: Enforced at the database level using `tenantScope`, `withTenant`, and `withTenantValues` helpers. `assertTenant` provides runtime isolation for hand-written queries.
- **API Codegen**: Orval generates Zod schemas and React Query hooks from `openapi.yaml`, with specific configuration rules to prevent build issues.
- **Drizzle Schema Convention**: Strict rules for Drizzle schema definitions to ensure compatibility with tier-review checks, avoiding inline options in column definitions.
- **API Server Middleware**: A carefully ordered middleware stack in `artifacts/api-server/src/app.ts` handles security, logging, rate limiting, tenant context, and error handling.
- **Tenant Context Plumbing**: `AsyncLocalStorage` manages tenant context, retrieved via `requireTenantContext()` in route handlers, facilitating a single-file change for authentication migration.
- **Rate Limiting**: Implemented with `defaultLimiter` (global) and `adminLimiter` (specific to GDPR routes).
- **Error Handling**: The `@workspace/errors` package provides a comprehensive error taxonomy, `getUserMessage` for user-facing messages, `TIMEOUTS` constants, `withTimeout` for fail-fast operations, `withRetry` for exponential backoff, and an in-house `CircuitBreaker`. `toApiError` provides universal error mapping.
- **Agent System**: A deterministic 6-stage agent loop (Router → Planner → Executor → Verifier → Research → Memory) handles complex operations.
- **Tool Registry**: A centralized registry of 15 tools with risk levels that gate the approval flow.
- **Privacy Logging**: Every outbound `fetch()` is bracketed by `logPrivacyEvent()`.
- **Filesystem Sandboxing**: `lib/sandbox.ts` restricts file operations to a dedicated workspace-specific directory, preventing path traversal and symlink attacks, and capping read/write sizes.
- **Authentication**: `bcryptjs`, `express-session`, and a DB-backed `sessions` table manage user authentication.
- **Database Schema**: SQLite with 19+ tenant-scoped tables — `tenants`, `workspaces`, `users`, `sessions`, `memories`, `agent_runs`, `messages`, `tool_calls`, `approvals`, `privacy_events`, `onboarding_profiles`, `model_preferences` (singleton-per-tenant primary-model + vision-lifecycle config), the Task #12 knowledge-base tables `kb_collections`, `kb_documents`, `kb_chunks`, the Task #10 `media_assets` table, the Task #25 legal-compliance tables `legal_acceptances` (append-only consent ledger), `incident_reports` (EU AI Act Article 73 channel), `age_confirmations` (COPPA / GDPR-K singleton), and the Task #15 `integrations` table (one row per tenant+provider, AES-256-GCM encrypted credentials).
- **Integrations**: `/api/integrations/*` exposes a static catalogue of 18 connectors (Notion, Slack, GitHub, etc.) with connect/disconnect/test/OAuth-stub/action endpoints. Credentials are encrypted via `lib/credential-crypto.ts` (AES-256-GCM, key from `OMNINITY_INTEGRATIONS_KEY` → `SESSION_SECRET`). Tier 1 actions return `{simulated:true,…}` envelopes; the real API adapters land in follow-ups. Provider actions are auto-registered into the tool registry as `<provider>.<action>`.
- **Onboarding & Updates**: `/api/onboarding/{profile,hardware,starter-tasks}` powers a 4-step wizard rendered at the operator shell when no completed profile exists. Hardware is probed via `os.*` with an `OMNINITY_HARDWARE_OVERRIDE` env seam for tests; the recommendation engine maps RAM/Apple-Silicon tier to a static MODEL_CATALOGUE. `/api/updates/check` compares `npm_package_version` to `OMNINITY_LATEST_VERSION` (env-driven seam for the future update server). The chat surface ships starter-task chips, a one-time first-approval tooltip, and a success-sparkle on first agent completion.
- **Platform Distribution & Code Signing (Task #27)**: Build assets in `build/` configure electron-builder for macOS Developer ID signing + hardened runtime + Apple notarization (`build/notarize.cjs` afterSign hook → notarytool + staple) and Windows EV code signing (`build/sign-windows.cjs` driving signtool with the EV cert thumbprint from a hardware token). `build/PrivacyInfo.xcprivacy` declares the required-reason APIs Apple has enforced since 2024; `build/entitlements.mac.plist` opens the minimum capabilities (screen recording, accessibility, microphone, network, JIT). `scripts/release-verify.sh` runs `spctl assess` + `codesign --verify` + `stapler validate` on .dmg, `signtool verify /pa /all` on .exe, and emits SHA-256 checksums + a `release-manifest.json` consumed by the auto-update client. Runtime surface lives at `/api/distribution/{build,permissions,permissions/:id}` (`services/distribution.service.ts`): the desktop shell reports its build attestation (signed/notarized/cert/sha256/etc.) and OS permission verdicts on launch; the server caches both per-tenant in memory (re-reported on next launch) and exposes `isFeatureGranted(tenant, feature)` so Desktop Control + Voice can graceful-degrade instead of crashing into a permission denial. Defaults are read from `OMNINITY_BUILD_*` env vars so the packaged binary populates the attestation at boot without any HTTP round-trip. Per-platform permission instructions + System Settings deeplinks ship for darwin / win32 / linux.
- **Hardware-Aware Model Recommendation (Task #64)**: `services/hardware/` owns the data-driven model catalogue (Phi-3 Mini → Mistral 7B / Qwen Coder 7B → Llama 3.1 8B → Llama 3.1 70B + Moondream 2 vision companion), the pure recommendation engine (`buildModelInstallPlan` / `evaluateMinimumSpec`), the in-process hardware cache, the vision-companion lifecycle state machine (aggressive/balanced/warm idle-timeout policy), and the singleton-per-tenant `model_preferences` service. Surfaced through `/api/models/{hardware,catalogue,recommended,select}`. The wizard's model step renders the recommended primary, alternative options keyed by use-case axis (writing/code/balanced), the bundled vision companion with idle-timeout toggle, total install size, and a min-spec screen when no model fits the host.
- **Knowledge Base (Task #12)**: Local second-brain with deterministic FNV-1a hash-bucket TF-IDF embeddings (256-dim, L2-normalised, no ML deps), sentence-aware chunking with overlap, sha256 dedupe, hybrid search (0.6·cosine + 0.4·Jaccard text overlap) with citations, collections/tags, URL ingest with `logPrivacyEvent` + `withTimeout` + AbortController + 1 MB cap + SSRF guard, and JSON export/import. RAG: `CreateAgentRunRequest.useKnowledgeBase` retrieves top-k snippets and injects them as a `system` message before the agent loop runs (best-effort — failures are swallowed). Routes mounted at `/api/knowledge`. UI lives at `/knowledge` in the operator app.
- **Tier-1 KB scope**: only `text`, `url`, and `youtube`-placeholder source types are wired; binary parsers (PDF, DOCX, EPUB, PPTX, CSV) are deferred until ML deps are introduced.
- **Media Generation Pipeline (Task #10)**: Local image/audio/video generation in `services/media.service.ts` writes real binary files (SVG / WAV / animated SVG) under the tenant sandbox at `media/<id>.<ext>`. Routes live at `/api/media/*` and binary streaming uses `application/octet-stream` to bypass the JSON envelope. A hardware probe (`/api/media/hardware`) selects between stub-only / mid-tier (FLUX/MusicGen) / high-tier (SDXL/AnimateDiff) models based on free RAM. Five new tools (`media.image.generate|upscale|removeBackground`, `media.audio.generate`, `media.video.generate`) are registered in the tool registry so the agent loop can request media. The Operator UI has a `Media` page with generation forms and a filterable library grid.
- **Multi-Task Queue (Task #38)**: `services/task-queue.service.ts` + `/api/tasks` drive a persistent queue of agent runs backed by `task_queue_entries`. The runner serializes every model invocation through a process-wide async mutex; concurrency is 1 on low-tier hardware (sequential mode) and 2 on mid+ tiers (parallel mode). Tasks support priority (high/normal/low), bumping, cancel, and `/tasks/clear` (requires `confirm: true`). Optional `contextSnapshot.requiredFiles[]` triggers a stale-context check at pickup — missing files flip the task to `stale` instead of running. Operator UI lives at `/queue` (sidebar entry "Tasks") with active / queued / recent panels and a confirm dialog for clear-all.
- **Legal & Compliance (Task #25)**: Static legal-document catalogue (`services/legal/documents.ts`) holds the canonical EULA, Privacy Policy, ToS, EU AI Act conformity statement, and open-source attribution; SHA-256 hashes pin the exact text accepted at every consent. `services/legal/model-licences.ts` is the single source of truth for bundled-model licence verdicts (FLUX.1-dev and MusicGen flagged `non_commercial_only` and excluded from the default bundle). `/api/legal/{documents,acceptances,acceptances/state,model-licences,incidents,age-confirmation}` surface the catalogue, the append-only consent ledger, the EU AI Act Article 73 incident channel, and the COPPA / GDPR-K age gate (US/UK 13+, EU/global 16+). The website footer links real `/legal/{privacy,terms,eula,eu-ai-act,open-source}` routes that render the same documents the API serves. The operator shell wraps both the onboarding wizard and the operator routes in `<LegalGate>` — no UI is reachable until pending legal acceptances are recorded and an age confirmation is on file.
- **Backups & Data Portability (Task #20)**: `services/backup.service.ts` produces AES-256-GCM-encrypted local archives (PBKDF2-SHA256 / 200k iterations / per-tenant 16-byte salt) framed by a fixed `OMOP-BAK\0|salt|iv|tag|ct` envelope, with snapshot version + schema version embedded for forward-compat. Snapshot covers memories, agent runs, messages/tool_calls/approvals, privacy events, onboarding, model preferences, desktop sessions/steps, the entire knowledge base (collections/documents/chunks), and backup_settings — `tenants`/`workspaces`/`users` are intentionally NOT exported (the destination already owns its tenant root). New `backup_settings` (singleton-per-tenant scheduling/retention/cloud-stub config) and `backup_jobs` (history) tables added in migration `0025_backups.ts` (renumbered during rebase from the original 0007 to land after the upstream migrations 0007–0024). Restore runs inside a single `db.transaction` (sync function, awaited promise — better-sqlite3 pattern). Cross-tenant restores regenerate primary keys and remap FKs (kbDoc→kbCollection, kbChunk→kbDoc, message/toolCall/approval→agentRun, approval→toolCall) so importing a coworker's archive doesn't collide with their still-live rows; same-tenant restores preserve IDs for idempotency. Scheduler is a pure tick (`findDueScheduledBackups(now)`); the caller supplies the password from the in-app prompt or OS keychain. Retention is enforced inline by `createBackup` and via the explicit `/api/backup/prune` endpoint. Cloud sync ships as a stub (icloud / googleDrive / dropbox / s3) — no real transport in v1; every "upload" still emits `backup.cloud_upload` to `privacy_events` so the audit trail and UI both reflect intent. Routes mounted at `/api/backup/*` (`settings`, `create`, `verify` — soft probe, returns 200 + `data.ok=false` on wrong password rather than 4xx, `restore`, `prune`, `jobs`, `export/full`) and `/api/export/*` (`conversations` JSON or Markdown, `memories` JSON, `settings` JSON). Schema versioning (`SNAPSHOT_VERSION` + `schemaVersion: 7`) gates restore so a future schema bump fails fast instead of silently dropping data. Desktop Backup UI deferred to a follow-up.
- **UI/UX**: `chart.tsx` uses hardcoded hex colors and `dangerouslySetInnerHTML`, which is slated for refactoring in Task #2 (Design System).
- **No-Code Skill Creator & Store (Task #4)**: Three-path wizard at `/marketplace/create` (Upload PDF/EPUB/DOCX/TXT/MD, Paste, 7-question Interview) drives `services/skill-draft.service.ts` which uses `ollama.service.chat` to extract → structure → produce a `SkillDraft` (with deterministic fallback when Ollama is unreachable, so tests never need a live model). Drafts are CRUD-able, support a live local-LLM tester, and can be published to the hosted Skill Store via `services/store.service.ts`. The store is co-hosted in the API process: tables `creator_accounts` (hashed API token, profile), `store_skills` (creator/slug/version triple, `isLatest` flag, `installCount`), `store_installations` (per-tenant install version for auto-update detection). Routes: `/api/skills/drafts/*` (tenant-scoped) and `/api/store/*` (public list/get + token-auth signup/publish/dashboard + tenant-auth install/updates). Privacy gate `requireStoreNetworkAccess()` blocks every store-network call unless `telemetryConsent.usageMetrics` or `productImprovement` is enabled (bypassable in tests via `OMNINITY_STORE_BYPASS_CONSENT=1`). Frontend pages: `marketplace.tsx` merges local + store + seed skills with source badges (`Local`, `From store`) and an `Update vN → vN+1` chip driven by `/store/skills/updates`; `marketplace-create.tsx`, `creator-signup.tsx`, `creator-dashboard.tsx` round out the creator portal. Drizzle convention reminder: required columns (`tenantId`, `createdAt`, `updatedAt`, `version`) MUST appear before any column declared with inline options like `{ mode: "boolean" }` or `{ mode: "json" }` — the tier-review regex stops body parsing at the first `}`.
- **Referral & Growth Mechanics (Task #35)**: Migration 0025 adds eleven tables — `referral_codes` (singleton-per-tenant), `referrals` (pending → completed lifecycle), `referral_rewards` (dual 30-day premium-skill access grants), `acquisition_channels` (singleton "how did you hear" survey), `share_events` (append-only share log), `task_satisfaction_ratings` (append-only thumbs after a run; positive ratings trigger the in-app share prompt), `creator_profiles` (singleton-per-tenant public portfolio; globally unique slug), `creator_milestones` (append-only "1k installs" achievements), `enterprise_trial_invites` (OP-for-Teams colleague invites), `waitlist_signups` (public marketing-site capture, stored under `SYSTEM_TENANT_ID`), and `beta_access_grants` (auto-granted at `BETA_REFERRAL_THRESHOLD = 3` completed referrals). Append-only tables omit `version` per Standard 6's audit-class carve-out. Services live in `services/{referrals,share,creator-profiles,waitlist}.service.ts`; routes mounted at `/api/{referrals,share,creators,waitlist}`. Onboarding completion (`upsertOnboardingProfile`) calls `completeReferralForReferred(ctx)` after the transaction so the referrer + referred dual-reward grant fires exactly once and a beta unlock is checked. Share cards are TEXT-only (URL/email tokens stripped) — never screenshots. Skill share links use `OMNINITY_PUBLIC_BASE_URL` (default `https://omninity.app`) for web URLs and the `omninity://` scheme for desktop deep-links.
- **Developer SDK & Plugin API (Task #14)**: In-process event bus (`artifacts/api-server/src/lib/event-bus.ts`) emits `task_started/completed/failed`, `tool_called`, `approval_requested/resolved`, `skill_installed/uninstalled/invoked`, `plugin_tool_registered/invoked` events with a bounded recent ring (100 / tenant) for polling. `services/plugin-tools.service.ts` + `/api/plugins/tools/*` register custom tools whose `invokeUrl` MUST be loopback (localhost / 127.0.0.1 / ::1 / *.localhost); the agent invokes them with the standard envelope, JSON-Schema-lite validation, optional bearer auth, and risk-level approval gating identical to first-party tools. `services/webhook-subscriptions.service.ts` + `/api/webhooks/subscriptions/*` are OUTBOUND subscriptions (distinct from `webhook.service.ts` which holds inbound HMAC secrets) — payloads are signed with `X-Omninity-Signature: sha256=…` and auto-disabled after 10 consecutive failures. `/api/events/recent` is the polling endpoint (afterId / type / limit). Storage: `plugin_tools` + `webhook_subscriptions` (migration 0026). The SDK package `@omninity/sdk` (`lib/sdk/`) exposes `OmninityClient` (runs/plugins/webhooks/events namespaces), an `events.stream()` async iterator, `createPluginSidecar()` HTTP helper, and `verifyEventSignature()` for HMAC verification. The CLI `op` (`lib/sdk-cli/`, bin via `pnpm --filter @omninity/sdk-cli op …`) supports `op run`, `op status`, `op skill create|publish|test`, `op plugin list|register|invoke`, `op events tail`. Docs in `docs/sdk/`.
- **Accessibility & i18n (Task #28)**: WCAG 2.1 AA foundations live in `artifacts/omninity-website/src/components/a11y/` (`SkipLink` + `MAIN_CONTENT_ID`, `LanguageSwitcher`) and `src/hooks/use-reduced-motion.ts`. Global rules in `src/index.css` enforce a `:focus-visible` ring fallback and a site-wide `prefers-reduced-motion` neutraliser. `src/i18n/` ships react-i18next 17 (no HTTP backend — bundles statically imported), the locale registry (en/es/fr/de/ja/ar with RTL flag), per-locale translation bundles validated against `TranslationShape` from `en.ts`, and `LocaleProvider` which mirrors the active locale onto `<html lang>`/`<html dir>`. Detection order: `localStorage` (key `omninity.locale`) → `navigator.language` → `<html lang>`. Translation key parity is enforced by `pnpm --filter @workspace/scripts run i18n-check` (`scripts/i18n-check.ts`) — fails CI if any non-English bundle is missing or has stray keys vs. the English source of truth.

## Testing & QA Pipeline (Task #18)

The QA stack is split into independent, composable scripts so any single check can run in isolation (locally, in CI, or as a pre-commit hook). All tooling is zero-dep — no vitest, c8, or Jest — built directly on `node:test`, `tsx`, and Node's built-in V8 coverage capture.

- **Lint** — `pnpm lint` runs `prettier --check` on the QA-pipeline source files and any other path opted in via `LINT_SCOPE` in `scripts/src/lint.ts`. The full codebase is not yet prettified (~250 legacy files would need a dedicated reformat commit); the scope is whitelist-based so new files stay clean from day one. `pnpm lint --fix` auto-formats. The structural half of "lint" (no `console.log`, no hex colours, no raw fetch, no `eval`, no `dangerouslySetInnerHTML`, no raw SQL, no unbounded module-level caches, schema invariants) is enforced by tier-review.
- **Unit / integration tests** — `pnpm test` runs every package's `test` script. The api-server runs both `test-runner.ts` (53+ integration cases) and `security.test.ts` (11 dedicated security regression cases: auth bypass, cross-tenant isolation on memory + agent runs, sandbox escape via `..` / absolute / NUL byte, prompt-injection rejection, approval bypass, admin rate-limit headers).
- **Performance benchmarks** — `pnpm bench` runs every package's `bench` script via the in-house `@workspace/scripts/bench-runner` helper. It emits `.bench-results.json` in vitest's `{files:[{tasks:[{name,result:{benchmark:{mean,p95}}}]}]}` shape so `tier-review` Check #9 can enforce `/** @budget Nms */` annotations directly above each `bench(...)` call. Currently covers `lib/db` (cursor encode/decode, `tenantScope` SQL build, `buildPage`) and `lib/errors` (`withTimeout`, `defaultShouldRetry`, `toApiError`).
- **Coverage** — `pnpm coverage` spawns each package's tests with `NODE_V8_COVERAGE` set, walks the per-process v8 JSON, computes line + function coverage from byte offsets, and writes `coverage/summary.{json,md}`. `pnpm coverage:check` enforces `COVERAGE_MIN_PCT` (default 80%) on `lib/**` packages. `@workspace/types` is excluded from the gate because it is almost entirely TypeScript declarations that erase at runtime.
- **Flaky test detector** — `pnpm flaky-detect` runs `pnpm test` per package N times (default 5) and flags any package whose result flips. Outputs `coverage/flaky.md` (human-readable) and `coverage/quarantine.json` (machine-readable list of quarantined packages with flake rate + timestamp, ready for CI annotations or dashboard ingestion). Wired as a nightly CI cron job, not a PR blocker.
- **Tier review** — `pnpm tier-review` runs the 21-check quality gate (typecheck, tests, no `console.log`, schema invariants, OpenAPI envelope, codegen-in-sync, generated `.d.ts` in sync, fetch privacy logs, perf budgets, bundle size, dangerous primitives, `dangerouslySetInnerHTML`, dependency audit, raw-SQL, tenant scoping, pagination envelope, required indexes, unbounded caches, translation key parity, axe-core accessibility). Append-only/audit-class tables are exempt from the `version` column requirement via `VERSION_EXEMPT_KEYWORDS` (audit, append, log, history, journal, event, invocation, screening, document, notice, collection) in `scripts/tier-review.ts`.
- **Audit-log concurrency** — `services/audit.service.ts` serialises `appendAuditEntry` per tenant via an in-process `Promise`-chain mutex (`tenantAppendLocks` Map keyed by `tenantId`). The hash chain's `readTip` + INSERT pair must be atomic; without serialisation, two concurrent appenders both read sequence N and both insert at N+1, producing duplicate sequence numbers and breaking `verifyAuditChain`. The mutex prunes its Map entry when the last waiter completes (identity-check on the materialised tail promise), so steady-state size is "tenants with an in-flight append", not "tenants ever seen". Local-first single-process scope only — multi-process deployments would need a DB-level unique `(tenant_id, sequence)` constraint instead.
- **QA orchestrator** — `pnpm qa` runs typecheck → lint → tests → coverage:check (optional, doesn't bail) → tier-review and prints a single coloured pass/fail summary. Supports `--skip=stage` and `--only=stage`.
- **CI** — `.github/workflows/ci.yml` runs typecheck, lint, tests, coverage, coverage:check, and tier-review on every PR with Node 24 + pnpm 10, uploads `coverage/` as an artifact, and appends `coverage/summary.md` to the PR's GitHub step summary. A separate nightly cron job (03:17 UTC) runs the flaky-test detector and uploads `coverage/flaky.md` + `coverage/quarantine.json`.
- **Voice Interface (Task #9)**: `/api/voice/{transcribe,synthesize,voices}` provides a Tier 1 stub Whisper-style STT (deterministic transcript) and a real WAV PCM16 TTS engine. Frontend `lib/voice-engine.ts` exposes `useVoiceRecorder` (MediaRecorder + AnalyserNode meter + Web Speech live captions), `useVoicePlayer` (HTMLAudioElement + interrupt), and `useWakeWord` (continuous SpeechRecognition). Chat page exposes mic button, waveform, voice-mode toggle and wake-word triggered capture; Settings exposes voice + speed + autoplay + wake-word controls and a preview button. All endpoints return the canonical envelope and are privacy-logged.

# External Dependencies

- **Database**: SQLite (via `better-sqlite3`)
- **ORM**: Drizzle ORM
- **Validation**: Zod (`zod/v4`), `drizzle-zod`
- **API Codegen**: Orval
- **Authentication**: `bcryptjs`, `express-session`
- **Logging**: `pinoHttp`
- **Security**: `helmet`, `cors`
- **Other Utilities**: `nanoid` (for request IDs), `path-to-regexp` (noted for high CVE, to be addressed by Task #16)